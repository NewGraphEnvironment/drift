% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dft_break_category.R
\name{dft_break_category}
\alias{dft_break_category}
\title{Temporal category of every row of a break-class summary}
\usage{
dft_break_category(x, years = NULL, rule = "v1")
}
\arguments{
\item{x}{Either the list returned by \code{\link[=dft_rast_break_class]{dft_rast_break_class()}} (its
\verb{$summary} and \verb{$years} are used) or a \verb{$summary}-shaped data frame, in
which case \code{years} is required. Columns \code{from_class}, \code{to_class},
\code{status} and \code{break_year} must be present.}

\item{years}{Integer vector of the observation years in the series. Ignored
with a warning when \code{x} is a \code{\link[=dft_rast_break_class]{dft_rast_break_class()}} result, which
carries its own.}

\item{rule}{Character. The labelling rule to apply. Only \code{"v1"} exists; it
is recorded in the returned \code{rule} column so old output identifies the
rule that produced it.}
}
\value{
\code{x}'s summary with three columns appended, its class and \strong{row
order} preserved (\verb{$summary} arrives sorted by \code{n_cells} descending):
\itemize{
\item \code{category} — a factor with the five levels below, in that order
\item \code{strength} — \code{\link[=dft_break_strength]{dft_break_strength()}}, \code{NA} off a clean switch
\item \code{rule} — the rule that produced \code{category}
}
}
\description{
Compose \code{\link[=dft_rast_break_class]{dft_rast_break_class()}}'s threshold-free measurements into the
labelled split the package and its consumers actually report: whether a
transition is a switch that held, a switch that turns on a single endpoint
observation, or a sequence that never settled — and, for the last of those,
whether the endpoints differ at all.
}
\details{
\code{dft_rast_break_class()} deliberately applies no threshold. Composing the
label is a judgement, so it lives here, in one place, under a version — see
\code{rule}. The numbers it rests on stay where they were measured.

\code{NA} propagates: a row whose pixels carry an \code{NA} in any interior year cannot
be scanned, arrives from \code{\link[=dft_rast_break_class]{dft_rast_break_class()}} with \code{status} \code{NA}, and is
labelled \code{NA} rather than refused. It is the only \code{NA} this function
produces, which is why an unlabelable class code is an error instead. The
bundled example series has no such row — mask a year, or classify a series
with a real \code{NA} in it, to see one.

A \code{status} value outside \code{stable} / \code{break} / \code{flicker}, or a \code{break} row
carrying no \code{break_year}, is an error rather than an \code{NA}: both mean the
frame did not come from \code{\link[=dft_rast_break_class]{dft_rast_break_class()}}.
}
\section{The \code{"v1"} rule}{

\tabular{rll}{
0 \tab \code{stable}         \tab \code{status == "stable"} \cr
1 \tab \code{break_sustained}\tab a clean switch, \code{strength >= 2} \cr
2 \tab \code{break_endpoint} \tab a clean switch, \code{strength == 1} \cr
3 \tab \code{unsettled}      \tab never settles, and the endpoints differ \cr
4 \tab \code{stable_flicker} \tab never settles, and the endpoints agree
}

Levels 3 and 4 are the reason this function exists. A two-epoch comparison
reports level 3 as change and cannot see level 4 at all, so they are used
differently and must never be added together: on the Bulkley floodplain that
is 2,032.9 ha against 3,186.5 ha, and summing them gives 7,811.5 ha of
"changed" area where the published layer says 4,625.0 — a 69\% overstatement.

\code{strength} is thresholded at 2 and the threshold is \strong{not} an argument: a
free threshold would mean \code{rule = "v1"} no longer identifies the labelling,
which is the composition problem this function exists to end. Use
\code{\link[=dft_break_strength]{dft_break_strength()}} directly for another cut.

\code{n_flips} is not part of this function's surface. It stays on \verb{$breaks},
where it is measured.
}

\examples{
years <- 2017:2023
rasters <- lapply(years, function(yr) {
  terra::rast(system.file("extdata", paste0("example_", yr, ".tif"),
                          package = "drift"))
})
names(rasters) <- years
res <- dft_rast_break_class(dft_rast_classify(rasters, source = "io-lulc"))

cat_tbl <- dft_break_category(res)
head(cat_tbl[c("from_class", "to_class", "category", "strength", "rule")])

# the two populations a four-level vocabulary pools, kept apart
tapply(cat_tbl$area, cat_tbl$category, sum)

# a summary read back from CSV needs the series stated
dft_break_category(as.data.frame(res$summary), years = years)$category[1:5]
}
\seealso{
\code{\link[=dft_rast_break_category]{dft_rast_break_category()}} for the same rule at pixel grain;
\code{\link[=dft_break_strength]{dft_break_strength()}} for the number it thresholds;
\code{\link[=dft_rast_break_class]{dft_rast_break_class()}} for the measurements.
}
