% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dft_rast_classify.R
\name{dft_rast_classify}
\alias{dft_rast_classify}
\title{Classify a raster with factor levels and colors}
\usage{
dft_rast_classify(x, class_table = NULL, source = "io-lulc", remap = NULL)
}
\arguments{
\item{x}{A \link[terra:SpatRaster-class]{terra::SpatRaster} or a named list of \code{SpatRaster}s (e.g. from
\code{\link[=dft_stac_fetch]{dft_stac_fetch()}}).}

\item{class_table}{A tibble with columns \code{code}, \code{class_name}, \code{color}
(hex). When \code{NULL}, loaded via \code{\link[=dft_class_table]{dft_class_table()}} using \code{source}.}

\item{source}{Character. Used to load a shipped class table when
\code{class_table} is \code{NULL}. One of \code{"io-lulc"} or \code{"esa-worldcover"}.}

\item{remap}{A named list for collapsing classes. Names are the new class
names, values are character vectors of original \code{class_name}s to merge.
For example, \code{list(Vegetation = c("Trees", "Rangeland"))}. When \code{NULL},
no remapping is applied.}
}
\value{
Same structure as \code{x} — a \code{SpatRaster} or named list — with
\code{\link[terra:factors]{terra::levels()}} and \code{\link[terra:colors]{terra::coltab()}} set.
}
\description{
Apply class names and a color table to a categorical \code{SpatRaster}, optionally
remapping (collapsing) classes into broader groups.
}
\examples{
r <- terra::rast(system.file("extdata", "example_2020.tif", package = "drift"))
classified <- dft_rast_classify(r, source = "io-lulc")
terra::is.factor(classified)
}
