% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dft_rast_summarize.R
\name{dft_rast_summarize}
\alias{dft_rast_summarize}
\title{Summarize area by class}
\usage{
dft_rast_summarize(x, class_table = NULL, source = "io-lulc", unit = "ha")
}
\arguments{
\item{x}{A \link[terra:SpatRaster-class]{terra::SpatRaster} or a named list of \code{SpatRaster}s. When a
named list, each name is used as the \code{year} column in the output.}

\item{class_table}{A tibble with columns \code{code}, \code{class_name}, \code{color}.
When \code{NULL}, loaded via \code{\link[=dft_class_table]{dft_class_table()}} using \code{source}.}

\item{source}{Character. Used to load a shipped class table when
\code{class_table} is \code{NULL}. One of \code{"io-lulc"} or \code{"esa-worldcover"}.}

\item{unit}{Character. Area unit for the \code{area} column. One of \code{"ha"}
(default), \code{"km2"}, or \code{"m2"}.}
}
\value{
A tibble with columns:
\itemize{
\item \code{year} (character, only when \code{x} is a named list)
\item \code{code} (integer)
\item \code{class_name} (character)
\item \code{color} (character, hex)
\item \code{n_cells} (integer)
\item \code{area} (numeric, in requested \code{unit})
\item \code{pct} (numeric, percentage of total non-NA cells)
}
}
\description{
Compute area and percentage by class for a categorical raster, optionally
across multiple years.
}
\examples{
r <- terra::rast(system.file("extdata", "example_2020.tif", package = "drift"))
dft_rast_summarize(r, source = "io-lulc", unit = "ha")
}
