% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dft_rast_break_category.R
\name{dft_rast_break_category}
\alias{dft_rast_break_category}
\title{Temporal category of every pixel in a break-class scan}
\usage{
dft_rast_break_category(x, rule = "v1", filename = NULL, overwrite = FALSE)
}
\arguments{
\item{x}{The list returned by \code{\link[=dft_rast_break_class]{dft_rast_break_class()}}. \verb{$breaks} supplies
the measurements and \verb{$raster} whether the endpoints differ. \verb{$years} is
\strong{not} needed here and not required: \code{n_before} and \code{n_after} are measured
per pixel, so the strength is read off them rather than recovered from
\code{break_year} the way \code{\link[=dft_break_category]{dft_break_category()}} must do at row grain. A result
saved before drift 0.16.0 therefore works unchanged.}

\item{rule}{Character. The labelling rule; only \code{"v1"} exists. Recorded on
the result as the \code{drift_break_rule} metadata tag, and implied by the
category level labels themselves.}

\item{filename}{Character or \code{NULL}. Where to write the result. A
floodplain-scale grid is worth putting somewhere you chose; \code{NULL} writes
a temporary file that R removes at the end of the session.

The \strong{file} carries the integer codes \code{0:4}, not the labels: the levels
are set on the returned object after the write, so no \code{.tif.aux.xml} RAT
sidecar is produced — one whose loss is silent, and which the rest of this
package avoids for that reason. \code{terra::rast(filename)} therefore returns a
plain integer raster; re-attach labels with the id order in \verb{@return}, or
keep the object this returns.}

\item{overwrite}{Logical. Replace \code{filename} if it already exists. terra
refuses by default and its error names this remedy, so the argument has to
exist for the message to be actionable.}
}
\value{
A two-layer \code{SpatRaster}:
\itemize{
\item \code{category} — a factor with ids \code{0:4} labelled \code{stable},
\code{break_sustained}, \code{break_endpoint}, \code{unsettled}, \code{stable_flicker}, in
that order
\item \code{strength} — \code{\link[=dft_break_strength]{dft_break_strength()}} as an integer, \code{NA} off a clean
switch
}

\code{NA} where the pixel could not be scanned, which is every pixel with an
\code{NA} in any year.
}
\description{
\code{\link[=dft_break_category]{dft_break_category()}} at pixel grain: the same \code{"v1"} rule, applied to the
per-pixel measurements rather than to the summary rows, so the label can be
mapped, patched, or crossed against anything else on the grid.
}
\details{
The rule, the reason levels 3 and 4 must never be summed, and why the
threshold is not an argument are all documented once, under
\code{\link[=dft_break_category]{dft_break_category()}}.
}
\section{Memory}{

One streamed \code{\link[terra:app]{terra::app()}} pass over \verb{$breaks} plus the transition layer,
written straight to \code{filename} — nothing full-grid is pulled into R, and the
chunk size is bounded rather than left to terra's memory heuristic, which
takes a 192M-cell grid in one or two chunks on a large machine.
}

\examples{
years <- 2017:2023
rasters <- lapply(years, function(yr) {
  terra::rast(system.file("extdata", paste0("example_", yr, ".tif"),
                          package = "drift"))
})
names(rasters) <- years
res <- dft_rast_break_class(dft_rast_classify(rasters, source = "io-lulc"))

cat_r <- dft_rast_break_category(res)
terra::plot(cat_r[["category"]])

# pixel grain and summary grain are the same rule, so they agree
terra::freq(cat_r[["category"]])
}
\seealso{
\code{\link[=dft_break_category]{dft_break_category()}} for the same rule over summary rows;
\code{\link[=dft_rast_break_class]{dft_rast_break_class()}} for the measurements.
}
