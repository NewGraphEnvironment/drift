% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dft_break_strength.R
\name{dft_break_strength}
\alias{dft_break_strength}
\title{How many observations a dated class switch held, either side}
\usage{
dft_break_strength(break_year, years)
}
\arguments{
\item{break_year}{Integer vector of break years, as reported in the
\code{break_year} layer of \verb{$breaks} or the \code{break_year} column of \verb{$summary}.
\code{NA} (a stable or flicker pixel) returns \code{NA_integer_}.}

\item{years}{Integer vector of the observation years in the series, sorted
and unique — \verb{$years} from \code{\link[=dft_rast_break_class]{dft_rast_break_class()}}, or the names of the
list passed to it.}
}
\value{
An integer vector the length of \code{break_year}, \code{NA} where
\code{break_year} is \code{NA}.
}
\description{
The confidence attached to a clean switch found by \code{\link[=dft_rast_break_class]{dft_rast_break_class()}}:
\code{pmin(n_before, n_after)}, recovered from \code{break_year} alone. A switch is
only as sure as its shorter side, so a pixel whose last observation alone
differs scores 1 however long the run before it was.
}
\details{
This is a \strong{measurement}, not a judgement — its meaning is fixed and it is
never versioned. \code{\link[=dft_break_category]{dft_break_category()}} thresholds it to produce a label,
which is versioned; a caller who wants a different threshold uses this
number directly rather than asking for another rule.

\code{break_year} is the first year of the new class, so a break at position
\code{idx} in the series has \code{n_before = idx} and \code{n_after = length(years) - idx}.
This inverts that: \code{idx = match(break_year, years) - 1}.

\strong{The unit is observations, not calendar years.} On a gapped series such as
\verb{2017, 2020, 2023}, a break at 2020 scores 1 — the same as an endpoint year
— even though three calendar years flank it on each side. A strength of 2 on
that series means "held for two observations", which is six years.

The maximum is \code{floor(length(years) / 2)}: 3 on the seven-year series drift
is usually run on, 1 on a four-year series, and 1 on the two- and three-year
series \code{\link[=dft_rast_break_class]{dft_rast_break_class()}} also accepts — so on a series shorter than
four observations no break can score 2, and \code{\link[=dft_break_category]{dft_break_category()}}'s
\code{break_sustained} level is unreachable rather than merely empty.

A \code{break_year} equal to \code{years[1]} is refused: the first year cannot be the
first year of a \emph{new} class, so a value there means the years and the breaks
came from different series. So is any value absent from \code{years}, which
\code{match()} would otherwise pass through as a silent \code{NA}, and so is a
fractional or non-numeric year, which \code{as.integer()} would truncate or blank.

\strong{\code{years} must be the series the breaks were measured over, exactly.} A
membership check cannot catch a \emph{superset}: on the true series \code{2017:2023} a
break at 2023 scores 1, and passing \code{2017:2030} instead scores it 6, turning
an endpoint-only switch into a sustained one with nothing to say so. Take
\code{years} from \code{dft_rast_break_class()}'s own \verb{$years} wherever the result is
to hand; a summary read back from CSV does not carry the series, so the
caller states it and owns being right about it.
}
\examples{
years <- 2017:2023

# the second and last years score 1 -- one observation on the short side
dft_break_strength(c(2018, 2019, 2020, 2021, 2022, 2023), years)

# NA in, NA out: a stable or flicker pixel has no break year
dft_break_strength(c(2020, NA), years)

# a gapped series scores in observations, not calendar years
dft_break_strength(2020, c(2017, 2020, 2023))
}
\seealso{
\code{\link[=dft_rast_break_class]{dft_rast_break_class()}}, which measures the switch;
\code{\link[=dft_break_category]{dft_break_category()}}, which thresholds this into a label.
}
