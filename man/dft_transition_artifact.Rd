% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dft_transition_artifact.R
\name{dft_transition_artifact}
\alias{dft_transition_artifact}
\title{Tag transition patches with geometric misregistration evidence}
\usage{
dft_transition_artifact(
  patches,
  transition,
  width_max = 1.5,
  boundary_dist_max = 1,
  reciprocal = TRUE,
  reciprocal_dist_max = 5,
  reciprocal_area_ratio_min = 0.5
)
}
\arguments{
\item{patches}{An \code{sf} object of transition patches from
\code{\link[=dft_transition_vectors]{dft_transition_vectors()}}, with columns \code{patch_id} (unique),
\code{transition} and \code{area_ha}. Run this \strong{before} zone attribution or
\code{\link[=dft_transition_attribute]{dft_transition_attribute()}} with \code{match_mode = "all"}: zone intersection
splits patch geometry and \code{"all"} mode duplicates \code{patch_id}, and both
change what "a patch" means to the metrics below.}

\item{transition}{The factor \code{SpatRaster} the patches were vectorized from
(the \verb{$raster} element of \code{\link[=dft_rast_transition]{dft_rast_transition()}}). Must be the
\strong{unfiltered} raster — one produced without \code{from_class} / \code{to_class} —
because the boundary signature reads the from-epoch class of the cells
\emph{around} each patch, and a filtered raster is \code{NA} there.}

\item{width_max}{Numeric, in pixels. A patch whose effective width
\code{2 * area / perimeter} is below this is flagged as a sliver. The default
\code{1.5} catches a one-pixel band (0.5–1 px by this metric) and clears a
two-pixel-wide strip.}

\item{boundary_dist_max}{Positive whole number, in pixels. A patch cell
counts as boundary-hugging when a from-epoch cell of the patch's \emph{to}
class lies within this many cells of it (a square window). \code{1} means
8-neighbour adjacency.}

\item{reciprocal}{Logical. Search for reciprocal partners? \code{FALSE} skips the
pairwise step and returns \code{NA} in the three \verb{reciprocal*} columns, for
very large patch sets where the search is not wanted.}

\item{reciprocal_dist_max}{Numeric, in pixels. How far from an \code{A -> B}
patch to look for a \code{B -> A} partner. Opposite-bank bands are separated
by the stable channel between them, so this must exceed the channel
width in pixels; the default \code{5} covers a 50 m river at 10 m.}

\item{reciprocal_area_ratio_min}{Numeric in \verb{[0, 1]}. A candidate partner
must have \code{min(area) / max(area)} at least this large — a compensating
shift moves about the same area in each direction.}
}
\value{
\code{patches} with eight columns appended (geometry stays last):
\itemize{
\item \code{width_m}, \code{width_px} (numeric) — effective width \code{2 * area / perimeter}
\item \code{flag_sliver} (logical) — \code{width_px < width_max}
\item \code{boundary_frac} (numeric, 0–1) — share of cells within
\code{boundary_dist_max} of the from-epoch interface; \code{NA} for stable patches
\item \code{flag_boundary} (logical) — \code{boundary_frac >= 0.5}; \code{NA} for stable
\item \code{reciprocal_id} — \code{patch_id} of the partner, or \code{NA}
\item \code{reciprocal_dist_m} (numeric) — distance to it (\code{0} when touching)
\item \code{flag_reciprocal} (logical) — a partner was found; \code{NA} for stable
patches or when \code{reciprocal = FALSE}
}
}
\description{
Year-to-year classified land cover carries geometric artifacts that read as
real transitions but are misalignment: a boundary that moved by a pixel
between epochs leaves a thin band of "change" along one side of it, and a
channel that shifted leaves \code{Trees -> Water} on one bank and
\code{Water -> Trees} on the other. Both inflate reported change, and both
concentrate on the long linear boundaries (banks, forest edges, field
margins) that riparian work is about. \code{patch_area_min} cannot separate them
from real change — a one-pixel band along a 5 km bank is 15 ha — because
area is width times length and the artifact is pathological in \emph{width}.
}
\details{
This function adds evidence columns to the patches from
\code{\link[=dft_transition_vectors]{dft_transition_vectors()}} and lets the caller decide. It drops nothing.
Every signature is derived from the transition raster alone, so it costs
no extra fetch and works on any factor transition raster.

Three signatures, each reported as a measurement plus a flag:

\strong{Sliver width.} Effective width \code{2 * area / perimeter} — for a rectangle
of sides \verb{a << b} this is close to \code{a}. On a 10 m grid a single pixel
scores 0.5 px, a 1 x 20 pixel band 0.95 px, a 20 x 20 block 10 px. Width
is orthogonal to \code{patch_area_min}: a 15 ha sliver and a 15 ha clearing are
identical on the area axis and ~80x apart on this one.

\strong{Boundary-hugging.} The share of a patch's cells that lie within
\code{boundary_dist_max} cells of a from-epoch cell of the patch's \emph{to} class —
that is, of the pre-existing interface between the two classes. This is the
disambiguator width alone cannot supply: a registration artifact \emph{traces}
an existing boundary, while a real thin change (a new road, a harvested
buffer strip) \emph{cuts across} one and scores near zero. \code{flag_boundary} is
\code{boundary_frac >= 0.5}.

\strong{Reciprocity.} Whether an \code{A -> B} patch has a \code{B -> A} partner of
comparable area within \code{reciprocal_dist_max}. Along a channel that shifted
by a pixel, one bank maps \code{Water -> Trees} and the other \code{Trees -> Water};
the net change is ~zero and the two bands are separated by the stable
channel, so the test is proximity rather than adjacency. The partner
recorded is the nearest one that meets the area criterion (ties to the
larger). This is a geometric relationship between two patches and is
unavailable to any per-pixel method, including the spectral check.

Stable patches (\code{A -> A}, present when \code{changes_only = FALSE}) get the
width columns — those are geometry only — and \code{NA} for everything else.
\code{NA} there means \emph{not applicable}, not "checked and clean".

No composed \code{flag_artifact} is returned: which signatures matter depends on
the AOI, and real changes are sometimes genuinely thin. Compose it yourself,
e.g. \code{flag_sliver & (flag_boundary | flag_reciprocal)}.

The independent confirmation route is spectral: \code{\link[=dft_rast_break]{dft_rast_break()}} on a
Sentinel-2 index cube, described in the \emph{Trajectories as a Check on
Land-Cover Change} vignette, where a mapped transition with no spectral
break is read as a label change rather than real change. That route needs
a cube and cannot see the reciprocal relationship; this one is free and
categorical-only. They are complements.
}
\section{Memory}{

The boundary signature is computed with streamed \code{terra} operations —
\code{segregate} into one layer per distinct \emph{to} class among the change
patches, a single \code{focal} pass over that stack, \code{rasterize} + \code{zonal} for
the per-patch fraction — with every intermediate written to a temporary
file so nothing full-grid is held in memory or pulled into R. Measured on
a 169M-cell floodplain grid (the BULK watershed group at 10 m). The
reciprocal search is an \code{sf} spatial-index query per transition pair.
}

\examples{
r17 <- terra::rast(system.file("extdata", "example_2017.tif", package = "drift"))
r23 <- terra::rast(system.file("extdata", "example_2023.tif", package = "drift"))
classified <- dft_rast_classify(list("2017" = r17, "2023" = r23), source = "io-lulc")
result <- dft_rast_transition(classified, from = "2017", to = "2023")
patches <- dft_transition_vectors(result$raster, changes_only = TRUE)

tagged <- dft_transition_artifact(patches, result$raster)

# most patches are slivers, but they hold a minority of the change area
table(tagged$flag_sliver)
tapply(tagged$area_ha, tagged$flag_sliver, sum)

# a sliver that also traces a pre-existing boundary is the artifact shape;
# compose the verdict yourself and keep the evidence
tagged$flag_artifact <- tagged$flag_sliver &
  (tagged$flag_boundary | tagged$flag_reciprocal)
head(sf::st_drop_geometry(tagged[tagged$flag_artifact, ]))

# the sliver that walks through patch_area_min = 5000 (0.75 ha, 1.4 px wide)
large <- dft_transition_vectors(result$raster, changes_only = TRUE,
                                patch_area_min = 5000)
large <- dft_transition_artifact(large, result$raster)
sf::st_drop_geometry(large[large$flag_sliver, ])
}
\seealso{
\code{\link[=dft_transition_vectors]{dft_transition_vectors()}} for producing the input patches and its
\code{patch_area_min} for the area axis; \code{\link[=dft_transition_attribute]{dft_transition_attribute()}} to tag
patches from an overlay layer once artifacts are flagged;
\code{\link[=dft_rast_break]{dft_rast_break()}} for the spectral confirmation route.
}
