% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dft_stac_classes.R
\name{dft_stac_classes}
\alias{dft_stac_classes}
\title{Extract class table from STAC item metadata}
\usage{
dft_stac_classes(items = NULL, source = "io-lulc")
}
\arguments{
\item{items}{An \code{rstac} items collection returned by \code{\link[rstac:request]{rstac::get_request()}}
or \code{\link[rstac:items_functions]{rstac::items_sign()}}. Uses the first item's properties.}

\item{source}{Character. Source name for CSV fallback lookup.
One of \code{"io-lulc"} or \code{"esa-worldcover"}. Only used when STAC
metadata lacks classification info.}
}
\value{
A tibble with columns \code{code} (integer), \code{class_name} (character),
\code{color} (character, hex), and \code{description} (character, may be NA).
}
\description{
Parses the \code{classification:classes} extension from STAC item properties
to build a class lookup table with codes, names, and colors.
}
\details{
Falls back to a shipped CSV if STAC metadata is missing or incomplete.
}
